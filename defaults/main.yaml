---
packer_build: false
use_minio_repository: false

# only supported when using the MinIO repository
enterprise: false

minio_ansible_group: 'minio'

minio_user: 'minio'
minio_group: 'minio'
minio_create_account: true
